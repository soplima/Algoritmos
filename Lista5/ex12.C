#include <stdio.h>


void vector_defrag(int n, int vet[n]){
    int vet1[];
    int vet2[];

    for(int i = 0; i < n; i ++){
        if (vet[i] != -1){
            vet1[i] = vet[i];
        }
        if(vet[i] = -1){
            int vet2[i] = vet[i];
        }vet1[i] = vet2[i];
    }
}